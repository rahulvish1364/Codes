#include <stdio.h>
#include<stdlib.h>

typedef struct Node {
  int data;
  struct node* link;

} Node;
struct Node* head=NULL;
void append(int a) //this means inserting the value in the node
{
  struct Node* temp;
temp = (struct Node*)malloc(sizeof(struct Node));
printf("Enter node data\n");
scanf("%d",&temp->data);
}
int main()
{
int x;
void append(x);
}
